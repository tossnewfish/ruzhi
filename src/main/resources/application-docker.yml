spring:
  datasource:
    url: jdbc:mysql://mysql:3306/onboarding?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root
  data:
    redis:
      host: redis
      port: 6379
      timeout: 2s

app:
  rocketmq:
    enabled: true
    namesrv-addr: rocketmq-namesrv:9876
